# PROMPT Chiến

Nền móng M0 của game đấu bot hình học.

```powershell
pnpm check
```

Lệnh này đọc schema, kiểm tra ví dụ bot/replay, kiểm tra ruleset số nguyên và xác nhận ranh giới phụ thuộc giữa các package.
